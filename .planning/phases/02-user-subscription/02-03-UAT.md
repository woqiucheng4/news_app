---
status: testing
phase: 02-user-subscription
plan: 02-03
source: 02-03-PHASE_SUMMARY.md, 02-03-API_CONTRACT.md, 02-03-VALIDATION.md
requirements: USER-07
started: 2026-05-29T11:47:00+08:00
updated: 2026-05-29T14:31:00+08:00
---

## Current Test

number: 1
name: 关键词命中标题
expected: |
  GET /articles/search?q=<标题中的词> 返回 200 数组，至少 1 条结果且 title 含该词
result: pass

## Prerequisites

- 后端服务已启动：`make docker-up` 或 `cd backend && uvicorn main:app --reload`
- 已执行演示数据 seed：`python backend/scripts/seed_demo_articles.py` 或 `make demo-seed`
- 测试客户端：`curl` / `httpie` / Postman / `/docs` Swagger UI
- 该端点**无需鉴权**（无需准备 token）

## Tests

### 1. 关键词命中标题
expected: GET /articles/search?q=AI 返回 200 数组，至少 1 条结果的 title 含 AI（不区分大小写）
result: pass
covers: USER-07
how_to: |
  curl "http://localhost:8000/api/v1/articles/search?q=AI&limit=20"

### 2. 关键词命中正文/摘要
expected: 用某只在 content 或 summary 中出现的词查询，仍能返回包含该词的文章
result: pass
covers: USER-07
how_to: |
  从 demo 数据中挑一个仅在 summary/content 出现的词
  curl "http://localhost:8000/api/v1/articles/search?q=<word>&limit=20"

### 3. 大小写不敏感
expected: q=ai 和 q=AI 和 q=Ai 返回相同的结果集（顺序与数量一致）
result: pass
covers: USER-07
how_to: |
  curl "http://localhost:8000/api/v1/articles/search?q=ai" > /tmp/lower.json
  curl "http://localhost:8000/api/v1/articles/search?q=AI" > /tmp/upper.json
  diff /tmp/lower.json /tmp/upper.json

### 4. 中文关键词搜索
expected: q=人工智能 或 q=苹果 返回 200，命中含该中文子串的文章
result: pass
covers: USER-07
how_to: |
  curl "http://localhost:8000/api/v1/articles/search?q=%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD"

### 5. 0 结果返回空数组
expected: q=zzzzzzzzzzzz_no_match 返回 200 且 body 是 [] (不报错)
result: pass
covers: USER-07
how_to: |
  curl -i "http://localhost:8000/api/v1/articles/search?q=zzzzzzzzzzzz_no_match"

### 6. 已删除文章被排除
expected: 数据库中 is_deleted=true 的文章即使 title 含查询词也不出现在结果里
result: pass
covers: USER-07
how_to: |
  在 DB 中将一篇匹配文章标记 is_deleted=true，再次搜索同一关键词，确认该文章消失
  pytest -q tests/test_api_endpoints.py -k search  # 自动覆盖时可在新建 test_article_search.py 中加用例

### 7. 按 published_at 倒序
expected: 多结果时第 1 条的 published_at >= 第 2 条 >= ... >= 第 N 条
result: pass
covers: USER-07
how_to: |
  curl "http://localhost:8000/api/v1/articles/search?q=AI&limit=10" | jq '.[].published_at'
  人工核对降序

### 8. limit 截断
expected: limit=5 时返回最多 5 条，limit=1 时返回最多 1 条
result: pass
covers: USER-07
how_to: |
  curl "http://localhost:8000/api/v1/articles/search?q=AI&limit=5" | jq 'length'
  curl "http://localhost:8000/api/v1/articles/search?q=AI&limit=1" | jq 'length'

### 9. 空查询 q= 被拒
expected: GET /articles/search?q= 返回 422，detail 中含 q 字段的 min_length 校验错误
result: pass
covers: USER-07
how_to: |
  curl -i "http://localhost:8000/api/v1/articles/search?q="

### 10. 缺失 q 参数被拒
expected: GET /articles/search 不带 q 参数返回 422，detail 中含 field required
result: pass
covers: USER-07
how_to: |
  curl -i "http://localhost:8000/api/v1/articles/search"

### 11. q 超长（>200 字符）被拒
expected: q=aaaa...(201个a) 返回 422，detail 中含 max_length 校验错误
result: pass
covers: USER-07
how_to: |
  curl -i "http://localhost:8000/api/v1/articles/search?q=$(python3 -c 'print("a"*201)')"

### 12. limit 越界被拒
expected: limit=0 或 limit=101 返回 422
result: pass
covers: USER-07
how_to: |
  curl -i "http://localhost:8000/api/v1/articles/search?q=AI&limit=0"
  curl -i "http://localhost:8000/api/v1/articles/search?q=AI&limit=101"

### 13. SQL injection 防护
expected: q="'; DROP TABLE articles; --" 返回 200 空数组或正常结果，articles 表未被破坏
result: pass
covers: USER-07
how_to: |
  curl "http://localhost:8000/api/v1/articles/search?q=%27%3B%20DROP%20TABLE%20articles%3B%20--"
  之后跑 GET /articles/trending 确认表仍存在

### 14. 公开端点（无鉴权）可访问
expected: 不带 Authorization 头访问 GET /articles/search?q=AI 仍返回 200
result: pass
covers: USER-07 (Auth contract)
how_to: |
  curl -i "http://localhost:8000/api/v1/articles/search?q=AI"
  确认 status 200 而非 401

### 15. ArticleResponse schema 完整性
expected: 每条结果含 id/title/url/source/published_at/created_at 等核心字段，类型与 02-03-API_CONTRACT.md 一致
result: pass
covers: USER-07
how_to: |
  curl "http://localhost:8000/api/v1/articles/search?q=AI&limit=1" | jq '.[0] | keys'

## Summary

total: 15
passed: 15
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

<!-- 测试发现的失败项以 YAML 形式 append 到这里。当前为空。 -->

## Notes

- 当前已补齐搜索语义自动化：`tests/test_article_search.py` 覆盖 title/content/summary 命中、大小写、中文、0 结果、删除排除、排序、limit 与 SQL injection 防护。
- Test 6 已由自动化场景覆盖（模拟 `is_deleted=true` 文章不出现在结果中），手工 DB 改写仅作为可选人工复核手段。
- Test 13 是安全回归测试，必须确保参数化查询保护生效（SQLAlchemy 的 `bindparam` / `text()` 参数模式不会被注入）。
- Test 14 验证当前的"无鉴权"设计决策，若产品策略改为需鉴权，本测试期望反转为 401。
- Test 4 的中文搜索仅做"子串匹配"，不做分词；命中"人工智能"但**不命中**"人工" + "智能"的 AND 组合。
  这是已知限制，详见 `02-03-API_CONTRACT.md` Search Semantics。
- 本轮已通过自动化证据转为 pass 的项：9/10/11/12/14（来源：与 `tests/test_api_endpoints.py` 同款 TestClient 校验，返回码分别为 422/422/422/422/200）。
- 批次 3 新增 pass：15（自定义 `FakeArticleServiceSchema` 返回单条文章，SearchResponse keys 校验通过）。
- 批次 4 已清零：1/2/3/4/5/6/7/8/13 通过自动化回归并转为 pass（新增 `tests/test_article_search.py` + 回归 `tests/test_api_endpoints.py`）。

## Recommended Test Order

1. **基础功能（Tests 1-4）**：标题/正文/大小写/中文命中
2. **结果集行为（Tests 5-8）**：0 结果、删除排除、排序、limit
3. **请求校验（Tests 9-12）**：422 边界
4. **安全（Test 13）**：SQL injection 防护回归
5. **契约确认（Tests 14-15）**：无鉴权 + schema 完整性

## Phase Index

- 收尾索引：[`02-03-PHASE_SUMMARY.md`](./02-03-PHASE_SUMMARY.md)
- 接口契约：[`02-03-API_CONTRACT.md`](./02-03-API_CONTRACT.md)
- 验证证据：[`02-03-VALIDATION.md`](./02-03-VALIDATION.md)
- Flutter 接入：[`02-03-FLUTTER_SEARCH_SCAFFOLD.md`](./02-03-FLUTTER_SEARCH_SCAFFOLD.md)
