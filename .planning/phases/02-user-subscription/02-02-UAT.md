---
status: testing
phase: 02-user-subscription
plan: 02-02
source: 02-02-PHASE_SUMMARY.md, 02-02-API_CONTRACT.md, 02-02-VALIDATION.md
requirements: USER-04, USER-05, USER-06
started: 2026-05-28T21:46:00+08:00
updated: 2026-05-29T14:24:00+08:00
---

## Current Test

number: 1
name: 关键词订阅幂等
expected: |
  连续两次同一 keyword 订阅，两次 response 的 topic.id 相同且不报唯一约束错误
result: pass

## Prerequisites

- 后端服务已启动：`make demo-up` 或 `cd backend && uvicorn main:app --reload`
- 已执行演示数据 seed：`python backend/scripts/seed_demo_all.py`（或 `make demo-seed`）
- 已知测试用户 ID（来自 `seed_demo_user_and_subscriptions.py`）
- 测试鉴权方式选其一：
  - **方式 A（推荐 QA）**：`x-user-id: <demo-user-uuid>` 兜底头
  - **方式 B（生产路径）**：`Authorization: Bearer <jwt>`
- 测试客户端：`curl` / `httpie` / Postman / `/docs` Swagger UI

## Tests

### 1. 浏览话题分类目录
expected: GET /subscriptions/topics/categories 返回 200 数组，每项含 name 与 topic_count
result: pass
covers: USER-04
how_to: |
  curl http://localhost:8000/api/v1/subscriptions/topics/categories

### 2. 按分类筛选话题列表
expected: GET /subscriptions/topics?category=tech&limit=20 返回 200，每项含 is_subscribed 字段且 category=tech
result: pass
covers: USER-04
how_to: |
  curl -H "x-user-id: <demo-user-uuid>" \
    "http://localhost:8000/api/v1/subscriptions/topics?category=tech&limit=20"

### 3. 关键词搜索话题
expected: GET /subscriptions/topics?q=AI 返回 200，至少 1 条话题 name 或 description 含 "AI"
result: pass
covers: USER-04
how_to: |
  curl -H "x-user-id: <demo-user-uuid>" \
    "http://localhost:8000/api/v1/subscriptions/topics?q=AI"

### 4. 查看话题详情与订阅态
expected: GET /subscriptions/topics/{id} 返回 200，含 is_subscribed；已订阅话题为 true，未订阅为 false
result: pass
covers: USER-04
how_to: |
  curl -H "x-user-id: <demo-user-uuid>" \
    "http://localhost:8000/api/v1/subscriptions/topics/<topic-id>"

### 5. 订阅已存在话题
expected: POST /subscriptions/subscribe 返回 200 success=true；GET /subscriptions/me 中可见该话题
result: pass
covers: USER-04, USER-06
how_to: |
  curl -X POST -H "x-user-id: <demo-user-uuid>" \
    -H "Content-Type: application/json" \
    -d '{"topic_id":"<topic-id>","push_enabled":true,"push_breaking_only":false}' \
    http://localhost:8000/api/v1/subscriptions/subscribe

### 6. 关键词订阅自动建话题
expected: POST /subscriptions/subscribe/keyword 返回 200，topic.slug 形如 keyword-nvidia 且 is_subscribed=true
result: pass
covers: USER-05
how_to: |
  curl -X POST -H "x-user-id: <demo-user-uuid>" \
    -H "Content-Type: application/json" \
    -d '{"keyword":"NVIDIA","category":"custom","push_enabled":true,"push_breaking_only":false}' \
    http://localhost:8000/api/v1/subscriptions/subscribe/keyword

### 7. 关键词订阅幂等
expected: 连续两次同一 keyword 订阅，两次 response 的 topic.id 相同且不报唯一约束错误
result: pass
covers: USER-05, USER-06
how_to: |
  重复执行 Test 6 两次，比对两次 response 的 topic.id

### 8. 查看我的订阅列表
expected: GET /subscriptions/me 返回 200 数组，每项含 topic / is_active / priority / push_enabled / subscribed_at
result: pass
covers: USER-06
how_to: |
  curl -H "x-user-id: <demo-user-uuid>" \
    http://localhost:8000/api/v1/subscriptions/me

### 9. 更新单条订阅设置
expected: PATCH /subscriptions/me/{id} 返回 200；后续 GET /me 中该项 priority=50, push_breaking_only=true
result: pass
covers: USER-06
how_to: |
  curl -X PATCH -H "x-user-id: <demo-user-uuid>" \
    -H "Content-Type: application/json" \
    -d '{"priority":50,"push_breaking_only":true}' \
    http://localhost:8000/api/v1/subscriptions/me/<topic-id>

### 10. 批量重排订阅优先级
expected: PUT /subscriptions/me/reorder 返回 200 updated=2；后续 GET /me 中两项 priority 与提交一致
result: pass
covers: USER-06
how_to: |
  curl -X PUT -H "x-user-id: <demo-user-uuid>" \
    -H "Content-Type: application/json" \
    -d '{"items":[{"topic_id":"<id-a>","priority":30},{"topic_id":"<id-b>","priority":20}]}' \
    http://localhost:8000/api/v1/subscriptions/me/reorder

### 11. 取消订阅
expected: DELETE /subscriptions/unsubscribe/{id} 返回 200；后续 GET /me 中该项消失或 is_active=false
result: pass
covers: USER-06
how_to: |
  curl -X DELETE -H "x-user-id: <demo-user-uuid>" \
    http://localhost:8000/api/v1/subscriptions/unsubscribe/<topic-id>

### 12. 取消后再订阅恢复历史
expected: 对 Test 11 取消的 topic 再 POST /subscribe，返回 200 不冲突；GET /me 中重新出现且 is_active=true
result: pass
covers: USER-06
how_to: |
  curl -X POST -H "x-user-id: <demo-user-uuid>" \
    -H "Content-Type: application/json" \
    -d '{"topic_id":"<刚取消的 topic-id>"}' \
    http://localhost:8000/api/v1/subscriptions/subscribe

### 13. 个性化 Feed 拉取
expected: GET /articles/feed?page=1&page_size=20 返回 200，body 含 page/page_size/articles/has_more
result: pass
covers: 联调（USER-06 + Phase 1 内容产物）
how_to: |
  curl -H "x-user-id: <demo-user-uuid>" \
    "http://localhost:8000/api/v1/articles/feed?page=1&page_size=20"

### 14. 未鉴权访问返回 401
expected: 不带任何鉴权头访问 GET /subscriptions/me 返回 401，body detail 含认证相关提示
result: pass
covers: Auth contract
how_to: |
  curl -i http://localhost:8000/api/v1/subscriptions/me

### 15. x-user-id 测试兜底有效
expected: 仅带 x-user-id 头访问 GET /subscriptions/me 返回 200，内容与该用户实际订阅一致
result: pass
covers: Auth contract（test fallback）
how_to: |
  curl -H "x-user-id: <demo-user-uuid>" \
    http://localhost:8000/api/v1/subscriptions/me

## Summary

total: 15
passed: 15
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

<!-- 测试发现的失败项以 YAML 形式 append 到这里。当前为空。 -->

## Notes

- 自动化测试已在 CI 通过（`pytest -q tests/test_user_subscription_api.py tests/test_api_endpoints.py`），
  本 UAT 主要补足**用户视角的端到端验收**，不替代单元/集成测试。
- 待 Flutter 工程落地后，本 UAT 应增补 UI 维度场景（订阅管理页操作、关键词输入、重排手势）。
- 测试 5/6/7/9/10/11/12 互有顺序依赖（同一测试 user 的订阅状态被反复修改），建议按编号顺序执行。
- Test 11/12 可揭示 02-02 的关键回滚能力（取消后历史恢复），是回归重点。
- 本轮已通过自动化证据转为 pass 的项：1/2/3/4/5/6/8/9/10/11/13/15（来源：`tests/test_user_subscription_api.py` + `tests/test_api_endpoints.py`）。
- 批次 3 新增 pass：14（`tests.test_user_subscription_api` 同款 TestClient 直调 `/subscriptions/me` 无头返回 401）。
- 批次 4 已清零：7（关键词幂等）与 12（取消后恢复历史）通过自动化回归并转为 pass（`tests/test_user_subscription_api.py`）。

## Phase Index

- 收尾索引：[`02-02-PHASE_SUMMARY.md`](./02-02-PHASE_SUMMARY.md)
- 接口契约：[`02-02-API_CONTRACT.md`](./02-02-API_CONTRACT.md)
- 验证证据：[`02-02-VALIDATION.md`](./02-02-VALIDATION.md)
