---
status: testing
phase: 02-user-subscription
plan: 02-01
source: 02-01-PHASE_SUMMARY.md, 02-01-API_CONTRACT.md, 02-01-VALIDATION.md
requirements: USER-01, USER-02, USER-03, USER-09
started: 2026-05-29T10:38:00+08:00
updated: 2026-05-29T14:48:00+08:00
---

## Current Test

number: 6
name: Google OAuth 登录
expected: |
  POST /auth/oauth/google 携带 provider_id+email 返回 200，body 含 access_token+user
result: pass

## Prerequisites

- 后端服务已启动：`make docker-up` 或 `cd backend && uvicorn main:app --reload`
- PostgreSQL 已迁移：`alembic upgrade head`
- 测试客户端：`curl` / `httpie` / Postman / `/docs` Swagger UI
- OAuth 真实联调（Tests 6-7）需配置 Google/Apple SDK；若仅做后端验收可使用自动化回归替代

## Tests

### 1. 邮箱注册成功
expected: POST /auth/register 用未注册邮箱返回 200，body 含 access_token / refresh_token / token_type=bearer / user
result: pass
covers: USER-01
how_to: |
  curl -X POST http://localhost:8000/api/v1/auth/register \
    -H "Content-Type: application/json" \
    -d '{"email":"alice@test.com","password":"abcdef","display_name":"Alice"}'

### 2. 邮箱重复注册被拒
expected: 用同一邮箱再次 POST /auth/register 返回 400，detail=Email already registered
result: pass
covers: USER-01
how_to: |
  重复执行 Test 1（同 email），断言返回 400

### 3. 邮箱密码登录成功
expected: POST /auth/login 用正确邮箱密码返回 200，body 含 access_token / refresh_token / user
result: pass
covers: USER-01
how_to: |
  curl -X POST http://localhost:8000/api/v1/auth/login \
    -H "Content-Type: application/json" \
    -d '{"email":"alice@test.com","password":"abcdef"}'

### 4. 邮箱密码登录失败
expected: 用错误密码 POST /auth/login 返回 401，detail=Invalid email or password
result: pass
covers: USER-01
how_to: |
  curl -i -X POST http://localhost:8000/api/v1/auth/login \
    -H "Content-Type: application/json" \
    -d '{"email":"alice@test.com","password":"wrong-password"}'

### 5. Refresh token 换发新 access
expected: POST /auth/refresh 携带 refresh_token 返回 200，body 含新 access_token / token_type=bearer
result: pass
covers: USER-01
how_to: |
  curl -X POST http://localhost:8000/api/v1/auth/refresh \
    -H "Content-Type: application/json" \
    -d '{"refresh_token":"<test1拿到的refresh>"}'

### 6. Google OAuth 登录
expected: POST /auth/oauth/google 携带 provider_id+email 返回 200，body 含 access_token+user
result: pass
covers: USER-02
how_to: |
  Flutter 客户端调 google_sign_in 拿 provider_id，再 POST 到后端
  或后端单测：pytest -q tests/test_auth_service.py -k oauth

### 7. Apple OAuth 登录
expected: POST /auth/oauth/apple 携带 provider_id+email 返回 200，body 含 access_token+user
result: pass
covers: USER-03
how_to: |
  iOS 真机 Apple Sign-In 拿 provider_id，再 POST 到后端
  或后端单测：pytest -q tests/test_auth_service.py -k oauth

### 8. 不支持的 OAuth provider 被拒
expected: POST /auth/oauth/github 返回 400，detail=Unsupported provider
result: pass
covers: USER-02, USER-03
how_to: |
  curl -i -X POST http://localhost:8000/api/v1/auth/oauth/github \
    -H "Content-Type: application/json" \
    -d '{"provider_id":"abc","email":"x@test.com"}'

### 9. OAuth 同邮箱已注册时合并账号
expected: 已用邮箱 alice@test.com 注册过，再走 google OAuth 同邮箱登录返回同一个 user.id
result: pass
covers: USER-02
how_to: |
  Test 1 后用相同 email 走 Test 6，比对返回的 user.id 与 Test 1 相同

### 10. 获取当前用户资料
expected: GET /users/me 携带 Bearer token 返回 200，body 含 id/email/display_name/is_active/is_verified/is_premium
result: pass
covers: USER-01
how_to: |
  curl -H "Authorization: Bearer <access_token>" \
    http://localhost:8000/api/v1/users/me

### 11. 未鉴权访问 /users/me 返回 401
expected: 不带 Authorization 头访问 GET /users/me 返回 401
result: pass
covers: USER-01
how_to: |
  curl -i http://localhost:8000/api/v1/users/me

### 12. 更新用户设置
expected: PUT /users/me/settings 携带 token 与 push_enabled+language+theme 返回 200 success=true
result: pass
covers: USER-01
how_to: |
  curl -X PUT -H "Authorization: Bearer <access_token>" \
    -H "Content-Type: application/json" \
    -d '{"push_enabled":true,"language":"zh-CN","theme":"dark"}' \
    http://localhost:8000/api/v1/users/me/settings

### 13. GDPR 数据导出
expected: GET /users/me/export 返回 200，body 含 user / settings / exported_at(ISO 时间)
result: pass
covers: USER-09
how_to: |
  curl -H "Authorization: Bearer <access_token>" \
    http://localhost:8000/api/v1/users/me/export

### 14. GDPR 注销账户
expected: DELETE /users/me 返回 200 success=true；之后用旧 token 访问 /users/me 返回 401 或 404
result: pass
covers: USER-09
how_to: |
  curl -X DELETE -H "Authorization: Bearer <access_token>" \
    http://localhost:8000/api/v1/users/me
  # 紧接着重试 GET /users/me 验证已失效

### 15. 注销后再注册同邮箱（边界）
expected: 软删除后用同一邮箱再注册的行为符合产品决策（成功创建新账号 或 返回邮箱已存在）
result: pass
covers: USER-09
how_to: |
  Test 14 之后再跑 Test 1，记录实际行为
  注：当前实现是软删除（is_active=false），重复注册可能仍返回 400

### 16. 密码长度过短被拒（pydantic 校验）
expected: POST /auth/register 携带 5 字符密码返回 422，detail 中含 password 长度校验错误
result: pass
covers: USER-01
how_to: |
  curl -i -X POST http://localhost:8000/api/v1/auth/register \
    -H "Content-Type: application/json" \
    -d '{"email":"short@test.com","password":"abcde"}'

### 17. 非法邮箱格式被拒
expected: POST /auth/register 携带 "not-an-email" 返回 422，detail 中含 email 格式校验错误
result: pass
covers: USER-01
how_to: |
  curl -i -X POST http://localhost:8000/api/v1/auth/register \
    -H "Content-Type: application/json" \
    -d '{"email":"not-an-email","password":"abcdef"}'

## Summary

total: 17
passed: 17
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

<!-- 测试发现的失败项以 YAML 形式 append 到这里。当前为空。 -->

## Notes

- 自动化测试已通过：`pytest -q tests/test_auth_api.py tests/test_auth_service.py tests/test_user_service.py tests/test_auth_user_uat_flow.py`
- 本 UAT 主要补足**用户视角的端到端验收**，不替代单元/集成测试
- Tests 6/7/9 已通过后端契约自动化覆盖（不依赖真实 SDK）；Google/Apple 真机联调仍建议作为 Flutter 侧补充验收。
- Test 15 是边界探测，揭示当前软删除实现下"重复注册"行为，结果不论 pass/issue 都应记录在 Notes 供产品决策参考
- Test 14 后该测试用户 token 不可再用，建议先跑完 Tests 10-13 再跑 14；Test 15 用新邮箱另起一组验证
- 本轮已通过自动化证据转为 pass 的项：1/2/3/4/5/8（来源：`tests/test_auth_api.py`）。
- 批次 3 新增 pass：16/17（使用 `tests.test_auth_api._build_app()` 的 TestClient，状态码均为 422）。
- 批次 4 已清零：6/7/9/10/11/12/13/14/15 通过自动化回归并转为 pass（新增 `tests/test_auth_user_uat_flow.py`）。

## Recommended Test Order

1. **基础链路（Tests 1-5）**：注册 → 登录 → 刷新 → 全部端点级
2. **OAuth 边界（Test 8）**：纯 backend 校验，无需真实 SDK
3. **资料 + 设置（Tests 10-12）**：用 Test 1 拿的 token 串起来
4. **GDPR（Tests 13-14）**：先导出后注销
5. **OAuth 真机（Tests 6-7, 9）**：需要 Google/Apple SDK 配置
6. **校验类（Tests 16-17）**：纯 schema 校验，可放最后

## Phase Index

- 收尾索引：[`02-01-PHASE_SUMMARY.md`](./02-01-PHASE_SUMMARY.md)
- 接口契约：[`02-01-API_CONTRACT.md`](./02-01-API_CONTRACT.md)
- 验证证据：[`02-01-VALIDATION.md`](./02-01-VALIDATION.md)
- Flutter 接入：[`02-01-FLUTTER_AUTH_SCAFFOLD.md`](./02-01-FLUTTER_AUTH_SCAFFOLD.md)
