# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0
jinja2>=3.1.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Cache
redis[hiredis]>=5.0.0

# AI
openai>=1.12.0
anthropic>=0.18.0

# Storage
aiofiles>=23.2.0
aioboto3>=12.3.0  # Optional: for S3

# Task Queue
celery[redis]>=5.3.0  # Optional: for distributed tasks
apscheduler>=3.10.4

# HTTP
httpx>=0.26.0
aiohttp>=3.9.0

# RSS
feedparser>=6.0.0

# HTML Parsing
selectolax>=0.3.0

# Utilities
python-jose[cryptography]>=3.3.0  # JWT
passlib[bcrypt]>=1.7.0  # Password hashing
python-multipart>=0.0.6  # Form data
structlog>=24.1.0  # Structured logging
datasketch>=1.6.5  # MinHash for deduplication
tenacity>=9.0.0  # Retry logic

# Push Notifications (optional — FCM)
firebase-admin>=6.5.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.26.0  # For testing
